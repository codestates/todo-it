// TODO: 파일의 디렉토리 구조 정리
export interface EnvironmentVariables {
  PORT: number;
  DB_HOST: string;
  DB_PORT: number;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
  JWT_SECRET: string;
}
